# Test environment using existing observability stack
# Note: Adjusted ports for existing services

# Sentry (would need actual Sentry - documenting the config)
SENTRY_DSN=http://test@localhost:9000/1
VITE_SENTRY_DSN=http://test@localhost:9000/1

# OTEL / Grafana Stack (using existing running services)
OTLP_ENDPOINT=http://localhost:4318
LOKI_OTLP_ENDPOINT=http://localhost:3100/otlp
SERVICE_NAME=effect-tanstack-start-sentry-test
NODE_ENV=development

# Disable Pyroscope for this test (not running)
# PYROSCOPE_SERVER_ADDRESS=http://localhost:4040
